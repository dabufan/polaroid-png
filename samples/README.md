# Samples

Place a few real `.polaroid.png` files here as test vectors.

Suggested:
- `v1_simple.polaroid.png`: pOLR v1 (embedded PNG payload)
- `v2_rle.polaroid.png`: pOLR v2 with RLE
- `v2_deflate.polaroid.png`: pOLR v2 with DEFLATE
